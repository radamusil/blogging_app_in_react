import React from 'react';
import ArticleList from '../ArticleList';


const Home = (props) => {
    return (
        <div>
            Recent articles

            <ArticleList />
        </div>
        );
}

export default Home;